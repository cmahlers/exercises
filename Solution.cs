using System;

//User function Template for C#

class Solution
{
    //Given  two integers N and M. The problem is
    //to find the number closest to N and divisible
    //by M. If there are more than one such number,
    //then output the one having maximum absolute value.
    public int closestNumber(int N, int M)
    {
        for (int i = 1; i <= M; i++)
        {
            if (N + 1 )
        }
    }
}